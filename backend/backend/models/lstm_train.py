"""
lstm_train.py
-------------

This module will later contain the full training pipeline for the LSTM model:
- loading historical data
- calculating indicators
- building the dataset
- training and saving the model.

For now, it only contains a placeholder function.
"""

def train_lstm_model():
    """
    Temporary placeholder training function.
    In the next steps, we will replace this with real code that:
    - loads data
    - builds sequences
    - trains an LSTM model
    - saves the model to disk
    """
    print("LSTM training placeholder – no real training yet.")


if __name__ == "__main__":
    # Simple manual test
    train_lstm_model()
